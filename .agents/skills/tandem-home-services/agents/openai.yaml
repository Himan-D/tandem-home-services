interface:
  display_name: "Tandem Home Services"
  short_description: "Repo-specific patterns and workflows for tandem-home-services"
  default_prompt: "Use the tandem-home-services repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true